import React, { Component } from 'react';

class Settings extends Component {
  render() {
    return (
      <div className="settings">
        <h1 className="settings-text" data-testid="settings-title">Settings</h1>
      </div>
    );
  }
}

export default Settings;
